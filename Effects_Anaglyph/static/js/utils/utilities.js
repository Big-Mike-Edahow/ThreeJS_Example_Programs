// utilities.js

// For the texturecube.
const path = "static/textures/";
const format = ".png";
export const urls = [
  path + "px" + format,
  path + "nx" + format,
  path + "py" + format,
  path + "ny" + format,
  path + "pz" + format,
  path + "nz" + format,
];

// For the sphere geometry.
export const spheres = [];
